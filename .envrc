# Canonical development-shell entry point for the product repository.
# .direnv/ is generated and ignored by Git.

nix_direnv_disallow_fallback
use flake

# Machine-specific paths stay outside the committed configuration.
if [[ -f .envrc.local ]]; then
  source_env .envrc.local
fi
